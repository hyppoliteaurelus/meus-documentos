Algoritmo "Fibonacci "



var
x, atual, anterior, novo:inteiro
inicio
   atual <- 1
   anterior <- 0
   escreval (atual)
   para x <- 1 ate 15 faca
   novo <- atual + anterior
   anterior <- atual
   atual <- novo
   escreval (novo)
   fimpara


fimalgoritmo


Fimalgoritmo